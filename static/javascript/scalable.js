//Scaling numbers
var scale_num_paste = 600;
var scale_num_modal = 600;

//Main loop
window.setInterval( function(){

	//Check page width and alter contents
	if(window.innerWidth < scale_num_paste){	
	}else if(window.innerWidth >= scale_num_paste){
	}

}, 100);
